name: Backend CI

on:
  push:
    branches:
      - main
      - develop
      - 'feature/**'
    tags:
      - 'v*'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: backend-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build Nacos backend
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Check backend source
        id: source
        shell: bash
        run: |
          if [ ! -f pom.xml ]; then
            echo "found=false" >> "$GITHUB_OUTPUT"
            echo "::warning::No pom.xml found in repository root. Push backend source first, then rerun this workflow."
            exit 0
          fi
          echo "found=true" >> "$GITHUB_OUTPUT"

      - name: Set up JDK 17
        if: steps.source.outputs.found == 'true'
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Build with Maven
        if: steps.source.outputs.found == 'true'
        shell: bash
        run: |
          set -euxo pipefail
          MVN="mvn"
          if [ -f ./mvnw ]; then
            chmod +x ./mvnw
            MVN="./mvnw"
          fi

          # This fork contains generated AgentKit Registry files. For CI packaging we skip Apache RAT
          # first, otherwise new generated source/resources without ASF headers stop the build before
          # compile checks. Later we can add license headers and enable RAT again.
          MAVEN_FLAGS="-B -ntp -DskipTests -Dmaven.test.skip=true -Drat.skip=true"

          # Nacos distribution builds normally use the release profile. If your fork removes it,
          # fall back to a normal Maven build so the workflow still gives useful feedback.
          $MVN $MAVEN_FLAGS -Prelease-nacos clean install || \
          $MVN $MAVEN_FLAGS clean install

      - name: Collect diagnostics on failure
        if: failure() && steps.source.outputs.found == 'true'
        shell: bash
        run: |
          set +e
          mkdir -p ci-diagnostics

          if [ -f target/rat.txt ]; then
            cp target/rat.txt ci-diagnostics/rat.txt
          fi

          find . -path '*/target/surefire-reports/*' -type f -print -exec cp --parents {} ci-diagnostics/ \; || true
          find . -path '*/target/failsafe-reports/*' -type f -print -exec cp --parents {} ci-diagnostics/ \; || true
          find . -path '*/target/*.log' -type f -print -exec cp --parents {} ci-diagnostics/ \; || true

          echo "Diagnostics files:"
          find ci-diagnostics -type f -maxdepth 10 -print || true

      - name: Upload diagnostics
        if: failure() && steps.source.outputs.found == 'true'
        uses: actions/upload-artifact@v7
        with:
          name: nacos-backend-diagnostics-${{ github.sha }}
          path: ci-diagnostics/
          if-no-files-found: ignore
          retention-days: 14

      - name: Collect build artifacts
        if: success() && steps.source.outputs.found == 'true'
        shell: bash
        run: |
          set -euxo pipefail
          mkdir -p ci-artifacts

          if [ -d distribution/target ]; then
            find distribution/target -type f \( -name '*.zip' -o -name '*.tar.gz' \) -print -exec cp {} ci-artifacts/ \; || true
          fi

          if [ -d console/target ]; then
            find console/target -maxdepth 1 -type f -name '*.jar' -print -exec cp {} ci-artifacts/ \; || true
          fi

          find . -path '*/target/*.jar' \
            ! -name '*sources.jar' \
            ! -name '*javadoc.jar' \
            ! -name 'original-*' \
            | sort | head -200 > ci-artifacts/jars.txt || true

          ls -lah ci-artifacts

      - name: Upload backend artifacts
        if: success() && steps.source.outputs.found == 'true'
        uses: actions/upload-artifact@v7
        with:
          name: nacos-backend-${{ github.sha }}
          path: ci-artifacts/
          if-no-files-found: warn
          retention-days: 14
